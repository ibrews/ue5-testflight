[ConsoleVariables]
; Workaround for UE-227478: Metal PSO precache builds vertex descriptor without
; attribute 7 (4th UV channel) but MetaHuman shader permutation 0x2308 declares
; it in stage_in — AGX hard-aborts. Disable precaching until Epic fixes this.
; Remove when UE-227478 is resolved upstream.
r.PSOPrecaching=0
